# Server
PORT=4000
NODE_ENV=development

# CORS - where your frontend runs from
FRONTEND_ORIGIN=http://localhost:5173

# Session / auth
# Generate: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
SESSION_SECRET=CHANGE_ME_LONG_RANDOM
COOKIE_NAME=flex_admin

# Admin login
ADMIN_USER=admin
# Generate: node -e "require('bcryptjs').hash('demo1234',10).then(console.log)"
ADMIN_PASS_HASH=$2a$10$PASTE_YOUR_BCRYPT_HASH_HERE

# Prisma (SQLite dev)
DATABASE_URL="file:./dev.db"
